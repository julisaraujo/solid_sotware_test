/// ColorMode enum
enum ColorMode {
  /// Solid color mode
  solid,
  /// Linear gradient color mode
  linearGradient,
  /// Radial gradient color mode
  radialGradient,
  /// Sweep gradient color mode
  sweepGradient
}
